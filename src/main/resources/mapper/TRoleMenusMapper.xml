<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.org.dianjiu.task.dao.TRoleMenusDao">

    <resultMap type="cn.org.dianjiu.task.entity.TRoleMenus" id="TRoleMenusResultMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="roleId" column="role_id" jdbcType="INTEGER"/>
        <result property="menuId" column="menu_id" jdbcType="INTEGER"/>
    </resultMap>

    <sql id="table_field">
      id, role_id, menu_id      
    </sql>
       
    <!--通过Id查询单个对象-->
    <select id="getById" resultMap="TRoleMenusResultMap" parameterType="java.lang.Integer">
        select
          <include refid="table_field" />
        from t_role_menus
        where id = #{id,jdbcType=INTEGER}
    </select>


    <!--通过实体不为空的属性作为筛选条件查询对象列表-->
    <select id="listByEntity" resultMap="TRoleMenusResultMap" parameterType="cn.org.dianjiu.task.entity.TRoleMenus">
        select
          <include refid="table_field" />
        from t_role_menus
        <where>
            <if test="id != null">
                and id = #{id,jdbcType=INTEGER}
            </if>
            <if test="roleId != null">
                and role_id = #{roleId,jdbcType=INTEGER}
            </if>
            <if test="menuId != null">
                and menu_id = #{menuId,jdbcType=INTEGER}
            </if>
        </where>
    </select>

    <!--通过实体不为空的属性作为筛选条件查询单个对象-->
    <select id="getByEntity" resultMap="TRoleMenusResultMap" parameterType="cn.org.dianjiu.task.entity.TRoleMenus">
        select
          <include refid="table_field" />
        from t_role_menus
        <where>
            <if test="id != null">
                and id = #{id,jdbcType=INTEGER}
            </if>
            <if test="roleId != null">
                and role_id = #{roleId,jdbcType=INTEGER}
            </if>
            <if test="menuId != null">
                and menu_id = #{menuId,jdbcType=INTEGER}
            </if>
        </where>
    </select>

    <!--通过Id列表作为筛选条件查询对象列表，列表长度不为0-->
    <select id="listByIds" resultMap="TRoleMenusResultMap" parameterType="list">
        select
          <include refid="table_field" />
        from t_role_menus
        where id in
        <foreach item="item" collection="list" separator="," open="(" close=")" index="index">
            #{item}
        </foreach>
    </select>

    <!--新增实体属性不为null的记录-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true" parameterType="cn.org.dianjiu.task.entity.TRoleMenus">
        insert into t_role_menus
        <trim prefix="(" suffix=")" suffixOverrides=",">
          <if test="id != null">
             id,
          </if>
          <if test="roleId != null">
             role_id,
          </if>
          <if test="menuId != null">
             menu_id,
          </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
          <if test="id != null">
             #{id,jdbcType=INTEGER},
          </if>
          <if test="roleId != null">
             #{roleId,jdbcType=INTEGER},
          </if>
          <if test="menuId != null">
             #{menuId,jdbcType=INTEGER},
          </if>
        </trim>
    </insert>

    <!--批量新增所有列，列表长度不能为0，且列表id统一为null或者统一不为null-->
    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true" parameterType="list">
        insert into t_role_menus
         (<if test="id != null">id,</if>role_id, menu_id)
        values
        <foreach item="item" collection="list" separator="," open="" close="" index="index">
         (<if test="item.id != null">#{item.id,jdbcType=INTEGER},</if>#{item.roleId,jdbcType=INTEGER}, #{item.menuId,jdbcType=INTEGER})
        </foreach>
    </insert>

    <!--通过主键修改实体属性不为null的列-->
    <update id="update" parameterType="cn.org.dianjiu.task.entity.TRoleMenus">
        update t_role_menus
        <set>
            <if test="roleId != null">
                role_id = #{roleId,jdbcType=INTEGER},
            </if>
            <if test="menuId != null">
                menu_id = #{menuId,jdbcType=INTEGER},
            </if>
        </set>
        where id = #{id,jdbcType=INTEGER}
    </update>

    <!--通过表字段修改实体属性不为null的列-->
    <update id="updateByField">
        update t_role_menus
        <set>
            <if test="where.roleId == null and set.roleId != null">
                role_id = #{set.roleId,jdbcType=INTEGER},
            </if>
            <if test="where.menuId == null and set.menuId != null">
                menu_id = #{set.menuId,jdbcType=INTEGER},
            </if>
        </set>
        <where>
            <if test="where.id != null">
                and id = #{where.id,jdbcType=INTEGER}
            </if>
            <if test="where.roleId != null">
                and role_id = #{where.roleId,jdbcType=INTEGER}
            </if>
            <if test="where.menuId != null">
                and menu_id = #{where.menuId,jdbcType=INTEGER}
            </if>
        </where>
    </update>

    <!--通过主键修改实体列表，列表长度不能为0，注意：当实体属性为null时，对应的列也会别更新为null-->
    <update id="updateBatch" parameterType="list">
        update t_role_menus
        <trim prefix="set" suffixOverrides=",">
            <trim prefix="role_id = case" suffix="end,">
                 <foreach collection="list" item="item" index="index">
                  when id = #{item.id} then #{item.roleId}
                 </foreach>
            </trim>
            <trim prefix="menu_id = case" suffix="end,">
                 <foreach collection="list" item="item" index="index">
                  when id = #{item.id} then #{item.menuId}
                 </foreach>
            </trim>
        </trim>
        where id in
        <foreach collection="list" index="index" item="item" separator="," open="(" close=")">
            #{item.id,jdbcType=INTEGER}
        </foreach>
    </update>
    
    <!--通过主键删除-->
    <delete id="deleteById" parameterType="java.lang.Integer">
        delete from t_role_menus where id = #{id,jdbcType=INTEGER}
    </delete>

    <!--通过实体非空属性删除-->
    <delete id="deleteByEntity" parameterType="cn.org.dianjiu.task.entity.TRoleMenus">
        delete from t_role_menus
        <where>
            <if test="roleId != null">
                and role_id = #{roleId,jdbcType=INTEGER}
            </if>
            <if test="menuId != null">
                and menu_id = #{menuId,jdbcType=INTEGER}
            </if>
        </where>
    </delete>
    
    <!--通过主键列表删除，列表长度不能为0-->
    <delete id="deleteByIds" parameterType="list">
        delete from t_role_menus where id in
        <foreach item="item" collection="list" separator="," open="(" close=")" index="index">
            #{item}
        </foreach>
    </delete>
    
    <select id="countAll" resultType="int">
        select count(id) from t_role_menus
    </select>
    
    <select id="countByEntity" parameterType="cn.org.dianjiu.task.entity.TRoleMenus" resultType="int">
        select count(id) from t_role_menus
        <where>
            <if test="id != null">
                and id = #{id,jdbcType=INTEGER}
            </if>
            <if test="roleId != null">
                and role_id = #{roleId,jdbcType=INTEGER}
            </if>
            <if test="menuId != null">
                and menu_id = #{menuId,jdbcType=INTEGER}
            </if>
        </where>
    </select>
</mapper>